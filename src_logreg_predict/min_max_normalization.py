"""
    Min-max normalization
    normalized_value = (value - min) / (max - min)

"""
def min_max_normalization(describe_dict, data):
    print("MIN MAX START")
    for head in data:
        print(head)
    print("MIN MAX END")
    
